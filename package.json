{
  "name": "jobportal-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node dist/production.js",
    "build": "esbuild server/production.ts --platform=node --packages=external --bundle --format=esm --outdir=dist",
    "postinstall": "npm run build"
  },
  "dependencies": {
    "express": "^4.21.2",
    "esbuild": "^0.25.0",
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.11",
    "typescript": "^5.6.3"
  }
}
